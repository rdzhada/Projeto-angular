import { DataBrasilPipe } from './data-brasil.pipe';

describe('DataBrasilPipe', () => {
  it('create an instance', () => {
    const pipe = new DataBrasilPipe();
    expect(pipe).toBeTruthy();
  });
});
